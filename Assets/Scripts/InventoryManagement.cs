using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InventoryManager : MonoBehaviour
{
    /*public GameObject inventoryPanel; // Referência ao Panel do seu inventário na UI
    public Sprite itemSprite; // Sprite do item que será adicionado ao inventário

    // Método para adicionar um item ao inventário
    public void AddItemToInventory()
    {
        // Cria um novo GameObject para o ícone do item
        GameObject itemIcon = new GameObject();
        
        // Adiciona um componente Image ao GameObject e define o sprite
        Image imageComponent = itemIcon.AddComponent<Image>();
        imageComponent.sprite = itemSprite;

        // Define o itemIcon como filho do inventoryPanel para que apareça na UI
        itemIcon.transform.SetParent(inventoryPanel.transform, false);
    }*/
}